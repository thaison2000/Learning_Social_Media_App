import TopBar from '../components/TopBar'

function Search() {
  return (
    <div>
        <TopBar/>
        <div>
            user.map(())
        </div>
        <div>

        </div>
    </div>
  )
}

export default Search